# Spring Boot 对接 USQL — 正确方式
# application.yml 正常配置真实数据库，Java 代码用 USqlDataSource 包装
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/login_db?useSSL=false&allowPublicKeyRetrieval=true
    username: login_user
    password: login123
