# Spring Boot + USQL — 标准配置
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/login_db?useSSL=false&allowPublicKeyRetrieval=true
    username: login_user
    password: login123
    driver-class-name: com.mysql.cj.jdbc.Driver
